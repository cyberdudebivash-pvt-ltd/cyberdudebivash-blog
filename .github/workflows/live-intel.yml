# DEPRECATED: Replaced by sentinel-apex.yml (v3.0)
# This workflow is disabled. All pipeline logic is now in sentinel-apex.yml.
name: "[DEPRECATED] Live Intel — Use sentinel-apex.yml"
on:
  workflow_dispatch:
    inputs:
      note:
        description: "This workflow is deprecated. Use sentinel-apex.yml instead."
        required: false
        default: "Use sentinel-apex.yml"
jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: "Deprecated"
        run: echo "This workflow is deprecated. Please use sentinel-apex.yml instead."
