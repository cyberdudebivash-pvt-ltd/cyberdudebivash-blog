# REPORTX Phase 1 — Capability Reconciliation Matrix

**Date:** 2026-08-19 (Round 1: PR #106, merged. Round 2: PR #107, merged — Phase 1D/1E/provider-reliability, see §7. Round 3: Phase 1F Key Judgements, see §9. Round 4: Phase 1H family-specific analytical engines, see §10. Round 5: Phase 1I ATT&CK/detection/hunting, see §11)
**Method:** Direct source-code inspection of all three report-generation systems this repository actually contains, cross-referenced against every prior certification record in `docs/audits/` and the repo root. Every row is grounded in a file path (and, where a claim is about *behavior* rather than *presence*, a real-data test run). Nothing below is inferred from a module or function name alone.
**Purpose:** Per the P0 continuation mandate's Section 4 — determine what genuinely needs to be built, as opposed to what the original prompt assumes should exist. This matrix is the deliverable that makes that determination auditable. **This is the current, live-updated version — §2's "after" column reflects state through Round 3, not just Round 1.**

## 0. The three systems (unchanged from `PRODUCTION-PIPELINE-AS-IS.md`, restated here for a self-contained reference)

| System | Path | Publishes to | Role |
|---|---|---|---|
| Pipeline A | `automation/` | `cyberbivash.blogspot.com` (Blogger API) | LLM-first with deterministic fallback; the pipeline this document's new work targets |
| Pipeline B | `fetch-live-intel.js` + `engine-node/`, `api/_lib/` | `blog.cyberdudebivash.in` (this repo's own `posts/*.html`, Vercel) | Fully deterministic, no LLM, independent codebase |
| CTI engine | `Sentinel-APEX/engine/sentinel_engine/reportx/` | Nothing directly — a library `automation/authority_transformer.py::_composer_enhance()` calls into | 27 modules, ~6,200 lines, 39+ test files; the most architecturally mature of the three, and — before this session — the least connected to what the public actually receives |

This document's scope is **Pipeline A and the CTI engine it already calls**. Pipeline B was not touched this session; its own gaps are fully documented in `PRODUCTION-PIPELINE-AS-IS.md` §2 and are not restated here.

## 1. State classification used below

`COMPLETE_AND_PRODUCTION_WIRED` · `IMPLEMENTED_NOT_WIRED` (a.k.a. "certified but dormant") · `PARTIALLY_IMPLEMENTED` · `SCHEMA_ONLY` · `TEST_ONLY` · `NOT_IMPLEMENTED` · `BLOCKED_EXTERNAL_DEPENDENCY`

A module existing is never sufficient for `COMPLETE_AND_PRODUCTION_WIRED`. The bar is: **a real production article, run through the actual `AuthorityTransformer.transform()` call path, demonstrably carries this capability's output** — proven this session by direct, non-mocked execution against representative CVE and ransomware-claim articles (script retained informally; results reproduced inline below), not by reading the code and assuming it runs.

## 2. Master capability matrix

| Mandate concept | State **before** this session | State **after** this session | Evidence |
|---|---|---|---|
| Production path reconstructed | `COMPLETE_AND_PRODUCTION_WIRED` | unchanged | `PRODUCTION-PIPELINE-AS-IS.md` (Phase 0) |
| Baseline captured | `COMPLETE_AND_PRODUCTION_WIRED` | unchanged | `PRODUCTION-BASELINE-2026-08-19.json` (Phase 0) |
| `FinishedIntelligenceObject` (single canonical contract) | `NOT_IMPLEMENTED` as one object; scattered across `ReportContext` + `EvidenceGraph` + `ReportBundle` + (new) `ProductTierVerdict` | `PARTIALLY_IMPLEMENTED` — the scattered pieces are now, for the first time, jointly observable on one object (`transform()`'s return dict) for every article, but still not unified into one versioned schema | This session, `automation/authority_transformer.py` |
| Claim graph exists (`claim_model.py`) | `IMPLEMENTED_NOT_WIRED` — real `Claim`/`EvidenceGraph`/`SourceRecord` classes, real `EpistemicState`/`CorroborationState` enums, 26 passing tests in `test_discovery_bridge.py` alone, built by `compose_report()` for **every** article, then discarded at the `_composer_enhance()` return boundary | `PARTIALLY_IMPLEMENTED` — now reaches `transform()`'s output as `result["evidence_graph"]` (real `claim_id`/`claim_type`/`status`/`evidence_refs`/`source_refs`/`corroboration_state`/`contradictions`, JSON-verified) | This session; see §3 below for the real-data proof, including one real defect found and fixed in the process |
| Claim status vocabulary | `IMPLEMENTED_NOT_WIRED` | `PARTIALLY_IMPLEMENTED` (now observable) | `claim_model.EpistemicState`: `CONFIRMED/REPORTED/CORROBORATED/ASSESSED/HYPOTHESIS/UNKNOWN/NOT_ASSESSED/NOT_APPLICABLE/DISPUTED` — a closer, more disciplined match to mandate Section 7 than a new schema would be (distinguishes *kinds* of standing, not one confidence scale, exactly as Section 11 demands) |
| Ransomware claim-boundary discipline (leak-site listing ≠ confirmed compromise) | `IMPLEMENTED_NOT_WIRED` | `PARTIALLY_IMPLEMENTED` (now observable) | `discovery_bridge.py::build_claims()`'s ransomware branch already keeps `c-victim-claim` at `REPORTED`, never `CONFIRMED`, with an explicit citation to the mandate's own Section 10 discipline in its own code comment — real-data-verified this session (see §3) |
| Corroboration engine (claim-level, not "2 URLs = corroborated") | `IMPLEMENTED_NOT_WIRED` | `PARTIALLY_IMPLEMENTED` (now observable) | `EvidenceGraph.recompute_corroboration()` computes `SINGLE_SOURCE`/`MULTI_SOURCE_INDEPENDENT`/`MULTI_SOURCE_DEPENDENT`/`UNCORROBORATED` from actual `independence_group` groupings, not source count. `MULTI_SOURCE_*` paths are unit-tested (`TestIndependentSourceCorroboration`, 7 tests) but no real article this session's real-data runs used had a second related post in the state file — the multi-source path is **still not independently re-verified against production data** |
| Contradiction engine | `IMPLEMENTED_NOT_WIRED` | `COMPLETE_AND_PRODUCTION_WIRED` (Round 2) — real hard gate, real no-op-today proof | `contradiction_engine.py`'s two checkers (dimension-level EpistemicState conflicts; rendered-text pattern conflicts) now run unconditionally inside `compose_report()` against the real graph and real rendered HTML, surface as `result["contradictions"]`, and hard-block publication via `validate_publication()` on any `severity=="block"` finding (all of them, by the engine's own design). Honestly empty for every real article tested (proven, not assumed — see §7.1: the dimension layer cannot yet fire because `discovery_bridge.py` never builds two competing same-dimension claims per article; wired anyway so it engages automatically the moment a future increment adds cross-source claim merging). This is no longer this document's top-named gap |
| Source reliability / information credibility / analytical confidence kept distinct | `IMPLEMENTED_NOT_WIRED` | `COMPLETE_AND_PRODUCTION_WIRED` (Round 2 for structured data; already rendered as prose before Round 2) | Three real, separately-computed axes: `source_reliability_grade()` (A-F), `information_credibility()` (number+label from `CorroborationState`), `overall_analytical_confidence()` (HIGH/MEDIUM/LOW from both). Rendered as 3 distinct prose lines since before this session; Round 2 additionally exposes all 3 as `result["analytical_confidence"]` structured data, not merely embedded text. No single field collapses them |
| Intelligence gaps | `NOT_IMPLEMENTED` as rich per-article analysis; a single static gap was computed and discarded | `PARTIALLY_IMPLEMENTED` — the one static gap ("independent corroboration not assessed") now reaches `transform()`'s output as `result["intelligence_gaps"]`; still not a rich, per-article gap analysis. Round 3: `report_contract.py`'s Section 21 state resolution corrected from `WITHHELD_INSUFFICIENT_EVIDENCE` to the `_PARTIAL_SIGNAL_ONLY` treatment (matching Section 13's precedent) — the section-state contract was understating a real, if minimal, capability that already existed since Round 2 | This session, `automation/authority_transformer.py`. Honest limitation: `analytic_scaffolding.IntelligenceGap` supports richer gaps (category, what-would-confirm-or-refute); `compose_report()` only ever populates the one |
| Key Judgements | `NOT_IMPLEMENTED` | `COMPLETE_AND_PRODUCTION_WIRED` (Round 3) — for LLM-authored narratives only; `LIVE_PROVIDER_VALIDATION_PENDING` | `automation/key_judgements.py` (new module): structured `KeyJudgement` synthesis request, strict independent validator (rejects unknown claim/evidence/source refs, unsupported high-impact assertions), wired into `transform()` gated on `content_source in LLM_AUTHORED_SOURCES`. Proven this round to be the genuine, final blocker to `PREMIUM_LONG_FORM` — see §9 |
| Entity normalization (ransomware actor/sector/country) | `COMPLETE_AND_PRODUCTION_WIRED` in `internal_linker.py`/`report_contract.py`/`report_renderer.py`; **`DUPLICATED_IMPLEMENTATION` and buggy** in `discovery_bridge.py` (read `article.labels`, not `article.ransomware_group` — see §4) | `COMPLETE_AND_PRODUCTION_WIRED` everywhere, reconciled | This session's fix — see §4 |
| Placeholder-actor guard (`"Unknown Group"`) | Present in `internal_linker.py` and `report_contract.py`; **absent** in `discovery_bridge.py` (it wasn't reading the right field to need one) | Present in all three (third copy added, matching the existing repo convention of duplicating this specific guard rather than adding cross-module coupling) | This session |
| Family-specific analytical engines | `PARTIALLY_IMPLEMENTED` — 2 of 9 real families (`cve_advisory`+`cisa_kev`+`cisa_advisory` share one matrix; `ransomware_claim` has its own) have a reconciled `report_contract.py` applicability matrix | `PARTIALLY_IMPLEMENTED`, materially wider — Round 4: 4 more (`ai_security`, `breach_notice`, `threat_actor`, `ransomware_reporting`) now have real matrices, role routing, and (for the 2 that had none) real narrative branches — 6 of 9 real families now covered | Round 4, see §10. `general_intelligence` remains deliberately unmapped (no real substantive-content gate exists to back a MANDATORY set honestly). Malware/phishing/zero-day/campaign are not real classifier outputs in this pipeline at all — see §10.4 |
| ATT&CK semantic validation (status + evidence + confidence) | `PARTIALLY_IMPLEMENTED`, `DUPLICATED_IMPLEMENTATION` across pipelines — Pipeline B's `detection-engine.js::mapTechniques()` already does evidence+confidence; Pipeline A's is prose-only, no status vocabulary | `PARTIALLY_IMPLEMENTED`, materially safer — Round 5: `attack_mapper.map_techniques()`'s evidence+confidence engine (already real, already used by a scorer) is now also a hard-gating check (`commercial_readiness.py`'s `detection_evidence_discipline`), not merely a display score. Still no formal `status` (OBSERVED/ASSESSED/CONDITIONAL/NOT_SUPPORTED) field reaching the rendered report — see §11.7 | Round 5, see §11 |
| Detection maturity model | `PARTIALLY_IMPLEMENTED`, two unreconciled systems (`report_renderer.DetectionPackage.status` reaches production; `detection_validation.DetectionValidationState` feeds the commercial-readiness scorecard only) | `PARTIALLY_IMPLEMENTED`, reconciled further — Round 5: all 4 real `DetectionPackage.status` strings now map to their own honest `DetectionValidationState` (previously 3 of 4 collapsed into one); the two systems are closer but still formally distinct vocabularies by design (one is a rendering-layer string, one is a governance enum) | Round 5, see §11 |
| Hunting hypotheses | `NOT_IMPLEMENTED` | `PARTIALLY_IMPLEMENTED` — Round 5: `executive_products.HuntHypothesis` (already real, already tested, never called) is now wired into the live pipeline for `cve_advisory` only, reaching rendered HTML, the intelligence-validation scorecard, and `report_contract.py`'s Section 14 state. `cisa_kev` and every other family still get none | Round 5, see §11 |
| Role-based decisions | `COMPLETE_AND_PRODUCTION_WIRED` for the 2 reconciled families, contextual (not generic boilerplate — `pipeline_composer._lean_role_decisions()`'s own comments document three separate, real, evidence-based prior fixes for exactly the generic-boilerplate defect class) | unchanged | Pre-existing; not touched this session |
| 24-section contract + tier gate (`report_contract.py` + `analytical_depth_gate.py`) | `IMPLEMENTED_NOT_WIRED` — built, 23 tests, `RELEASE_CERTIFIED_WITH_LIMITATIONS`, explicitly documented as "not wired into `authority_transformer.py`'s live `transform()` call" | `COMPLETE_AND_PRODUCTION_WIRED` for observability + the `FLASH` floor **and, as of Round 3, the ceiling**: `PREMIUM_LONG_FORM` is now genuinely reachable end-to-end for an LLM-authored narrative with validated Key Judgements and independent corroboration — proven by a real (mocked-provider, since no live LLM access exists in this sandbox) end-to-end test, not merely by construction. This is the first time in this codebase's history the tier ceiling has been demonstrated reachable | Round 1 (§3), Round 3 (§9.1 — `test_premium_long_form_is_genuinely_reachable_end_to_end`) |
| Unsupported-claim / fabrication fail-closed gates | `COMPLETE_AND_PRODUCTION_WIRED` for pattern-based checks + `achieved_tier` | `COMPLETE_AND_PRODUCTION_WIRED`, two more axes added: `product_tier == FLASH` (Round 1) and any `severity=="block"` contradiction (Round 2) | `automation/report_integrity.py::validate_publication()` |
| Provider failure handling (429 vs. 402, backoff, jitter, Retry-After) | `NOT_IMPLEMENTED` — confirmed by full-file read of `llm_client.py`: every exception, regardless of HTTP status, immediately moved to the next provider with zero retry, zero backoff, zero `Retry-After` parsing | `COMPLETE_AND_PRODUCTION_WIRED` (Round 2) for the one provider capable of succeeding today | `automation/llm_client.py::_call_openai_compat()` now retries only on HTTP 429, bounded (2 retries, ≤10s each, real `Retry-After` honored when sent, else backoff+jitter), never retries 402/other errors. Live-canary evidence (`PHASE-0-RELEASE-CERTIFICATION.md`) showed exactly this shape: Groq succeeds then 429s seconds later, DeepSeek/OpenRouter 402 (account billing, not retryable) — this directly targets the one retry that can actually help. Bounded deliberately against the triggering workflow's real 5-minute ceiling |
| Certified-artifact-hash binding | `NOT_IMPLEMENTED` | `COMPLETE_AND_PRODUCTION_WIRED` (Round 2) | `report_integrity.compute_artifact_hash()` (SHA-256 of the exact certified HTML) is computed inside `transform()` right after `validate_publication()` passes, returned as `result["certified_artifact_hash"]`, and re-verified in `main.py::run_pipeline()` immediately before the Blogger publish call — a mismatch raises the existing `PublicationIntegrityError` (reusing the established gate/retry-queue path, not a new one). Proven a no-op today (content is never mutated between certification and publish — verified by code trace, not assumed) via a real end-to-end adversarial test that tampers the hash and confirms `publisher.publish_post()` is never reached |
| Blogger hard gate | `PARTIALLY_IMPLEMENTED` — `validate_publication()` is real and unconditional, but is schema/pattern/tier-based, not the mandate's literal 7-condition AND (claim-traceability and semantic-QA are not separately implemented checks) | Two more conditions added: `FLASH` (Round 1), contradictions + artifact-hash binding (Round 2). Claim-traceability-as-its-own-check and semantic QA (SUPPORTED/ASSESSED_WITH_BASIS/UNSUPPORTED/CONTRADICTED per material claim) remain not separately implemented | This session |
| Post-publication fetch-back | `NOT_IMPLEMENTED` | unchanged | Confirmed absent by Phase 0's research; not attempted this session (no live publish occurred) |
| Live canary | One real canary executed and proven in Phase 0 (Groq fix only) | unchanged | Not re-run this session — would require triggering `workflow_dispatch` against production secrets, out of scope for a same-session code change |

## 3. Real-data proof this session's wiring is genuinely live (not merely present)

Two representative articles were run through the actual, non-mocked `AuthorityTransformer.transform()` — a clean CVE (`cve_advisory` family) and a ransomware victim claim (`ransomware_claim` family), the same two families `report_contract.py` has a reconciled applicability matrix for.

**CVE article** — `product_tier: "TACTICAL"`, reason: `"2/14 mandatory sections withheld for insufficient evidence: key_judgements, intelligence_gaps"`. `evidence_graph`: 1 source, 5 evidence records, 5 claims, including `c-cve-id` at `status: "CONFIRMED"` (NVD is authoritative for its own ID assignment) and `c-exploitation-status` at `status: "UNKNOWN"` (this record's evidence doesn't confirm exploitation — never inferred from CVSS alone, matching mandate Section 17's explicit prohibition).

**Ransomware claim** — `product_tier: "TACTICAL"`, reason: `"3/14 mandatory sections withheld for insufficient evidence: key_judgements, business_impact, intelligence_gaps"`. `evidence_graph`: 4 claims, including `c-victim-claim` (`status: "REPORTED"`, text: *"A leak-site listing names this organization as a victim..."* — never `CONFIRMED`, matching the mandate's Section 18 boundary exactly) and `c-actor-attribution` naming the real actor.

Both results were re-derived by hand against `report_contract.py`'s actual applicability matrix before running (14 mandatory sections for each family; only Key Judgements/Intelligence Gaps — and, for ransomware, Business Impact — have no implementation anywhere in this pipeline) and matched exactly. This also independently reconfirms `docs/audits/REPORTX-24-SECTION-LONG-FORM-RELEASE-CERTIFICATION.md`'s own finding that no real report reaches `PREMIUM_LONG_FORM` today — now proven through the live wiring, not only through that document's own mocked/reconstructed tests.

## 4. Real defect found and fixed during this session's real-data testing

`discovery_bridge.py`'s ransomware-claim actor attribution (`build_claims()` and, identically, `build_threat_product()`) derived the actor name from `next(label for label in article.labels if label != "ransomware")` — **`article.labels` carries site taxonomy tags every article gets** (this repository's own `"CYBERDUDEBIVASH"` brand label, `"Threat Intelligence"`, etc.), not actor names. The canonical, already-sanitized field is `article.ransomware_group` (established by the Phase 1 `related_intelligence[]` slice, and already correctly used by `internal_linker.py`, `report_renderer.py`, and `analytical_depth_gate.py::_resolve_actor_context()`) — `discovery_bridge.py` was the one caller in the whole pipeline still reading the wrong field.

This was invisible before this session because nothing consumed `graph.claims`/`build_threat_product()`'s output in production — the exact "capability exists but the public report never benefits from it" defect class the P0 mandate's Section 6 names directly, and here it hid a real correctness bug, not just dead weight. In this session's own test article the bug surfaced as: attributing a ransomware claim to **"CYBERDUDEBIVASH"** (this site's own brand) instead of the real actor. Fixed by reading `article.ransomware_group`, with the same `"Unknown Group"` placeholder guard already established in `internal_linker.py`/`report_contract.py` (a third, deliberate copy — this file already keeps itself dependency-light from the other two, per its own header comment). Two new regression tests added (`test_actor_attribution_uses_ransomware_group_not_the_site_taxonomy_label`, `test_unknown_group_placeholder_does_not_become_a_named_actor_attribution`); full engine suite re-run clean (929/930 — the one failure is pre-existing and unrelated, see §6).

## 5. Highest-leverage next increment (named, not implemented as of Round 2)

**Superseded (Round 1's pick, done in Round 2):** wiring `contradiction_engine.py` — see §7.1.

**Superseded (Round 2's pick, done in Round 3):** Key Judgements. Round 2 classified this `BLOCKED_EXTERNAL_DEPENDENCY` on the theory that a live LLM call was required to verify a modified prompt template. Round 3 corrected that reasoning per the mandate's own explicit instruction: implementation, unit tests (deterministic fixtures), integration tests (mocked `call_llm`), and adversarial tests (malformed JSON, prompt injection, fabricated high-impact claims) do not require live provider access — only the final live-canary confirmation does, and that is now tracked separately as `LIVE_PROVIDER_VALIDATION_PENDING` rather than blocking all engineering work. See §9.

**Superseded (Round 3's pick, done in Round 4): family-specific analytical engines, partially.** 4 of the 5 previously-unmapped real families (`ai_security`, `breach_notice`, `threat_actor`, `ransomware_reporting`) now have a reconciled matrix, role routing, and real narrative content — see §10. `general_intelligence` remains deliberately unmapped (real, named reason, not an oversight).

**Superseded (Round 4's pick, done in Round 5): ATT&CK semantic validation / detection-maturity reconciliation, partially.** See §11 — the detection-maturity vocabulary is reconciled, the ATT&CK-citation check is now a hard gate, and `cve_advisory` has real hunting. A formal structured ATT&CK status object, `cisa_kev`/other-family hunting, and full detection-maturity-state semantic QA remain.

**Current pick: real evidence extraction for a genuinely new report family (mandate Phase 1H, remainder).** Malware/phishing/zero-day/campaign do not exist as classifier outputs today and have zero structured evidence fields anywhere in `DiscoveredArticle` — building one for real (not fabricated) would mean designing and wiring new evidence extraction from raw article text, a materially different kind of work than reconciling an existing matrix against existing capability. Not started; named here rather than guessed at. A structured ATT&CK status object (§11.7) is the other real candidate of comparable size.

**Note on Phase 1G (entity resolution):** substantially already satisfied by Round 1's fix (see the `Entity normalization` and `Placeholder-actor guard` rows above) — `internal_linker.py`, `report_contract.py`, and `discovery_bridge.py` are reconciled on a single canonical field (`article.ransomware_group`) with a consistent placeholder guard. Any further Phase 1G work is scoped to widening this to non-ransomware entity types (CVE-referenced product/vendor names, threat-actor-article subject normalization), which is really a sub-case of the family-completeness work above, not a separate increment.

**Runner-up (unchanged from Round 1/2):** rendering `evidence_graph`'s claims into the actual public HTML body as a claim-by-claim ledger, rather than the composer's existing narrative-summary paragraph. Still a content/UX decision this document does not make unilaterally.

## 7. Round 2 additions (Phase 1D, 1E, provider reliability, artifact binding)

### 7.1 Contradiction engine — now wired

`contradiction_engine.py`'s two checkers (dimension-level `EpistemicState` conflicts via `find_dimension_contradictions()`; rendered-text pattern conflicts via `find_text_contradictions()`) now run unconditionally inside `compose_report()` (`pipeline_composer.py`) against the real evidence graph and the real rendered HTML, surface as `result["contradictions"]` in `transform()`'s output, and hard-block publication in `validate_publication()` on any `severity=="block"` finding — every contradiction this engine can currently construct, by its own module-docstring design ("Gate: unresolved_contradictions == 0").

**Honest limitation, proven not assumed:** the dimension-level layer cannot yet find a real conflict, because `discovery_bridge.py` never builds two competing claims for the same canonical dimension within one article (single-source construction; an independent-source match upgrades an existing claim's `corroboration_state` rather than adding a second, competing claim — see `_apply_independent_corroboration()`'s own docstring). `_dimension_tags_for()`'s 1:1 claim_id→dimension mapping makes this structurally provable, not merely untested: verified via 4 real-article tests (`TestDimensionTagsFor`) plus 2 zero-contradiction real-article tests (`TestContradictionWiring`), all passing. The wiring is real and load-bearing anyway — it engages automatically the moment a future increment adds cross-source claim merging, without a second wiring pass, and the text-pattern layer is already live against whatever real prose `compose_report()` renders today.

### 7.2 Analytical confidence — 3-axis structure exposed

`executive_products.py`'s `source_reliability_grade()`/`information_credibility()`/`overall_analytical_confidence()` were already computed and rendered as 3 distinct prose lines before Round 2 (discovered mid-session: `test_rendered_report_shows_three_separate_labeled_reliability_lines` already existed and passed pre-Round-2). Round 2's addition is exposing the same 3 real values as `result["analytical_confidence"]` structured data for the first time, cross-checked against the rendered prose in the same test.

**Genuinely absent, not attempted:** `assumptions[]` as a distinct analytical concept (mandate Section 14 / new-mandate Section 4). Confirmed by repository-wide grep (`class Assumption|assumptions\[|ASSUMPTION`) returning zero hits anywhere in `Sentinel-APEX/`. Building this from scratch — genuine assumption *identification*, not just a new empty list — is new analytical capability, not wiring, and was not attempted this round.

### 7.3 Provider reliability — bounded 429 retry, real backoff/jitter/Retry-After

`llm_client.py` had **zero** retry/backoff/jitter/`Retry-After` handling before Round 2 — confirmed by full-file read: every exception, 429 and 402 alike, immediately moved to the next provider. Now `_call_openai_compat()` retries specifically on HTTP 429 (bounded: 2 retries, ≤10s each, honors a real `Retry-After` header when the provider sends one, else exponential backoff with jitter), and continues to never retry 402 or any other error — matching the live-canary evidence in `PHASE-0-RELEASE-CERTIFICATION.md` exactly (Groq succeeds then 429s within seconds; DeepSeek/OpenRouter 402 on account billing, not retryable). Bounded deliberately against the triggering workflow's real 5-minute ceiling and up-to-5-posts-per-run volume (`PRODUCTION-PIPELINE-AS-IS.md` §2.5). 13 new tests, all using dependency-injected `sleep_fn` so the suite verifies real backoff-timing logic without a single real sleep (full run: 0.20s for 28 tests).

**Not independently re-verified against live Groq/DeepSeek this session** — this sandbox has no configured provider API keys or outbound network access to those endpoints. The mechanism is proven correct via HTTP-mock-level tests (status codes, headers, call counts) reproducing the exact conditions Phase 0's own live canary observed, not via a fresh live call.

### 7.4 Certified artifact hash binding

`report_integrity.compute_artifact_hash()` (SHA-256 of the exact certified HTML) is computed inside `transform()` immediately after `validate_publication()` passes and returned as `result["certified_artifact_hash"]`. `main.py::run_pipeline()` recomputes the same hash over the exact `transformed["content"]` about to be sent to Blogger, immediately before the `publish_post()` call, and raises the existing `PublicationIntegrityError` (no new exception type, no new retry-queue path — reuses what's already there) on any mismatch.

Verified two ways: (1) code trace confirms `transformed["content"]` is never mutated between `transform()`'s return and the publish call today, so this is provably a no-op currently — the same "safe because unreachable today, protective the moment it becomes reachable" pattern as the FLASH and contradiction gates; (2) a real end-to-end adversarial test (`test_certified_artifact_hash_mismatch_blocks_publication`) runs the actual `run_pipeline()` with a deliberately tampered hash and confirms `publisher.publish_post()`'s endpoint is never called, `published == 0`, and every article is recorded `integrity_blocked`.

## 8. Known limitations of this document

- Pipeline B (`fetch-live-intel.js`) was not re-investigated; everything about it is carried over from `PRODUCTION-PIPELINE-AS-IS.md` §2 unchanged.
- The multi-source corroboration path (`MULTI_SOURCE_INDEPENDENT`/`MULTI_SOURCE_DEPENDENT`) is unit-tested but was not exercised by any real-data run this session (no second related article existed in any test's state file).
- One pre-existing, environment-dependent test failure (`Sentinel-APEX/engine/tests/test_certification.py::test_certify_real_end_to_end_with_the_actual_node_rendering_check`, `Sentinel-APEX/renderer/certify-rendering.js`) was present before this session touched anything and is unrelated to any change in this document's scope — not investigated or fixed.
- No live canary or Blogger publish/fetch-back was executed this session (would require triggering production `workflow_dispatch` — a decision this document does not make unilaterally).
- Provider-reliability changes (§7.3) are verified against mocked HTTP responses reproducing Phase 0's own documented live-canary conditions, not against a fresh live call in this session.
- **Round 3 addition:** Key Judgements (§9) is verified the same way — deterministic fixtures and mocked `call_llm` responses, including adversarial/malformed/injection cases — never against a live provider in this sandbox. Recorded explicitly as `LIVE_PROVIDER_VALIDATION_PENDING`, not silently assumed working. `PREMIUM_LONG_FORM` reachability (§9.1) is proven with the same mocked-provider methodology — the *mechanism* is proven, not a live customer-facing article at that tier.

## 9. Round 3 additions (Phase 1F — Key Judgements)

### 9.1 Key Judgements — implemented, validated, wired, and proven to unlock `PREMIUM_LONG_FORM`

New module `automation/key_judgements.py` implements the mandate's structured `KeyJudgement` object (`id`, `judgement`, `confidence`, `claim_refs[]`, `evidence_refs[]`, `source_refs[]`, `reasoning_basis`, `decision_relevance`, `assumptions[]`, `limitations[]`, `contradictions[]`, `intelligence_gaps[]`, `what_would_change_the_judgement`) as a frozen dataclass with `to_dict()` serialization. Three functions carry the pipeline: `build_key_judgement_prompt()` (isolates the article's source material behind explicit `>>> UNTRUSTED SOURCE DATA` / `<<< END UNTRUSTED SOURCE DATA` markers and an explicit instruction not to follow any embedded directives, and lists the real claim/evidence/source IDs the model is allowed to reference), `parse_key_judgements_response()` (strict JSON parsing, strips markdown fences, never raises — returns `None` on any malformed input), and `validate_key_judgements()` (the independent, non-LLM-trusting gate).

`validate_key_judgements()` rejects, per candidate, on eight distinct machine-readable codes: `MALFORMED_CANDIDATE`, `MISSING_JUDGEMENT_TEXT`, `MALFORMED_CONFIDENCE`, `MALFORMED_CLAIM_REFS`, `UNKNOWN_CLAIM_REFERENCE`, `UNKNOWN_EVIDENCE_REFERENCE`, `UNKNOWN_SOURCE_REFERENCE`, `UNSUPPORTED_HIGH_IMPACT_CLAIM` — the last one pattern-matching the mandate's own prohibited-fact list (confirmed exploitation, breach, data theft, attribution, financial impact, customer exposure, zero-day, ransom payment) and requiring non-empty `claim_refs` before such a judgement is accepted. One malformed candidate never blocks acceptance of the others in the same response. Every ref is checked against the real `evidence_graph` passed in — a judgement citing a claim/evidence/source ID that doesn't exist in this article's actual graph is rejected, not trusted.

Wired into `authority_transformer.py::transform()`, gated on `content_source in LLM_AUTHORED_SOURCES` (the same existing constant `evaluate_product_tier()` already uses — no duplicate authorship check introduced) and on a non-empty `evidence_graph`. Accepted judgements render into the public HTML body via a new `_render_key_judgements_html()` (reusing the existing `_section()`/`_bullets()`/`_esc()` renderer primitives — zero new HTML-generation logic) and are counted into `evaluate_product_tier(..., key_judgement_count=len(key_judgements))`, which `report_contract.py`'s `evaluate_section_states()` now resolves Section 3 (Key Judgements) as `COMPLETE` when `key_judgement_count > 0`, `WITHHELD_INSUFFICIENT_EVIDENCE` otherwise — the section state reflects the real, validated count, never the raw LLM output.

**The decisive proof:** `tests/test_authority_transformer.py::TestKeyJudgementsWiredIntoTransform::test_premium_long_form_is_genuinely_reachable_end_to_end` demonstrates, for the first time in this codebase's history, `AuthorityTransformer.transform()` resolving `product_tier: "PREMIUM_LONG_FORM"` with `product_tier_mandatory_withheld: []`, real validated `key_judgements`, zero contradictions, and a verified `certified_artifact_hash` — using a mocked LLM response (this sandbox has no live provider access) shaped exactly like what `parse_key_judgements_response()`/`validate_key_judgements()` actually require, run through the real, unmocked validator. This closes the gap Round 2's §5 named `BLOCKED_EXTERNAL_DEPENDENCY`: the block was in reasoning, not in the code — the tier ceiling needed real implementation work, which live-provider access was never actually a prerequisite for.

### 9.2 Two real defects found and fixed during Round 3's own testing

1. **Late-binding default-argument bug** (production code defect): `generate_key_judgements(..., call_llm_fn=call_llm)` bound the parameter's default to the `call_llm` function object at module-import time — the standard `unittest.mock.patch("automation.key_judgements.call_llm", ...)` idiom used everywhere else in this codebase silently failed to intercept calls made through that default, because Python evaluates defaults once, not per-call. Fixed by changing the signature to `call_llm_fn: Optional[callable] = None` with `call_llm_fn = call_llm_fn or call_llm` resolved fresh from the module namespace inside the function body. Caught by 4 of 5 new end-to-end tests failing with `key_judgement_rejections: ["LLM_UNAVAILABLE"]` despite a patched mock; root-caused via standalone script reproduction, not guessed from the traceback alone.
2. **Test fixture missing `source_url`** (test-only defect): the new `_write_prior_independent_post()` helper initially omitted `source_url` from its synthetic state-file entry. `discovery_bridge.py::_apply_independent_corroboration()` constructs the corroborating `SourceRecord` with `url=match.get("source_url") or ""`, and `commercial_readiness.py`'s `source_provenance` control correctly fails-closed on a source with no URL — this was the pipeline correctly rejecting genuinely incomplete test data, not a pipeline bug. Fixed by adding a realistic `source_url`, matching what production's `PublicationState.mark_published()` always writes.

Both are documented in full, with root-cause narrative, in `docs/audits/REPORTX-PHASE1F-KEY-JUDGEMENTS-CERTIFICATION.md`.

### 9.3 Section 21 (Intelligence Gaps) section-state correction

Bundled into this round as a one-line, directly-related fix: `report_contract.py`'s `_PARTIAL_SIGNAL_ONLY` frozenset (previously containing only `SECTION_13_HISTORICAL_CORRELATION`) now also contains `SECTION_21_INTELLIGENCE_GAPS`, matching the precedent already established for Section 13 — a real, article-independent mechanism exists (`compose_report()` has unconditionally computed one static intelligence gap since Round 2) but wasn't reflected in the section-state contract, which still resolved `WITHHELD_INSUFFICIENT_EVIDENCE` for every article regardless. This was blocking `PREMIUM_LONG_FORM` for a reason unrelated to Key Judgements at all — found while diagnosing why Key Judgements alone didn't unlock the tier in the first attempt at the decisive test in §9.1.

### 9.4 Test evidence

30 new tests in `tests/test_key_judgements.py` (prompt construction, response parsing, validator — including two dedicated prompt-injection-resistance tests — and orchestration), plus 6 new tests in `tests/test_authority_transformer.py::TestKeyJudgementsWiredIntoTransform` (not-attempted-when-not-LLM-authored, generated-and-rendered, malformed-response-fails-closed, fabricated-high-impact-claim-rejected, the decisive PREMIUM_LONG_FORM proof, and an HTML-escaping/XSS adversarial test on rendered judgement text). Full automation-side suite: 465/465 passing. Full engine-side suite: 938/939 passing (the 1 failure is the same pre-existing, environment-dependent Node-rendering issue noted in §8, confirmed present before this round and unrelated to it).

### 9.5 Live provider validation status

**`LIVE_PROVIDER_VALIDATION_PENDING`** — recorded explicitly, not omitted and not fabricated, per this round's own governing instruction. All of the above is verified against deterministic fixtures and mocked `call_llm` responses; no live Groq/DeepSeek/OpenRouter/Anthropic call has exercised `key_judgements.py` in this sandbox (no provider credentials or outbound network access to those endpoints exist here). The repository's existing `workflow_dispatch`-triggered live-canary GitHub Actions workflow (the same one Phase 0 used) is the correct, already-authorized mechanism to close this gap — a specific next action, not a blocker to the engineering work itself, exactly as this round's mandate distinguishes `IMPLEMENTATION BLOCKED` from `LIVE PROVIDER VALIDATION PENDING`.

## 10. Round 4 additions (Phase 1H — family-specific analytical engines, partial)

Full detail: `docs/audits/REPORTX-PHASE1H-FAMILY-ENGINES-CERTIFICATION.md`.

### 10.1 The real defect this round targets

`analytical_depth_gate.evaluate_product_tier()` caps any family with no `_FAMILY_APPLICABILITY` entry at `TACTICAL` unconditionally (`mandatory` is always empty, since `get_applicability()` defaults every section to `OPTIONAL`) — regardless of evidence quality or LLM authorship. Before this round, that was 5 of the 9 real classifier outputs. A second, independent bug was found in the same audit: `threat_actor`/`ransomware_reporting` were missing from `report_renderer._detection_package()`'s `not_applicable` family set and fell through to CVE-shaped vulnerability-class branches that never match either, landing on the correct final state for the wrong, generic reason.

### 10.2 What was built

Real, reconciled `_FAMILY_APPLICABILITY` matrices (`ai_security`/`breach_notice`/`ransomware_reporting` share one 13-MANDATORY-section shape; `threat_actor` gets its own, keeping Section 12 OPTIONAL rather than MANDATORY since `_resolve_actor_context()` cannot yet recognize its evidence — see §10.3), the detection-status fix, two new `_family_analysis()` narrative branches (`threat_actor`, `ransomware_reporting` — previously zero differentiation for either), real unconditional role routing for all 4 (reusing the existing `RoleAudience` enum, zero new roles), and real family-conditioned intelligence gaps additive to the pre-existing universal one.

### 10.3 A dependency this round surfaced: Section 19 is only honestly MANDATORY because role routing is unconditional

`_resolve_implemented_section()` resolves Section 19 (Role Decision Matrix) to `COMPLETE` unconditionally once `Applicability != NOT_APPLICABLE` — it never actually checks whether `_lean_role_decisions()` returned anything. This already existed, unexamined, for `cve_advisory`/`ransomware_claim` (safe only because their role routing has always been unconditional too). The 4 new families' role decisions were deliberately designed the same way (never evidence-gated) specifically so this round's new `MANDATORY` claim on Section 19 is true in substance, not merely claimed. Not fixed at the mechanism level (would require threading a real role count through `evaluate_section_states()`, a larger, separate change) — named here as a real, pre-existing characteristic this round extended rather than introduced.

### 10.4 What this round explicitly does not do

No new report families (malware/phishing/zero-day/ransomware_campaign/campaign/IOC-bulletin/strategic-intelligence — none exist as classifier outputs, and `DiscoveredArticle` has no structured evidence fields for any of them); no `general_intelligence` matrix (the true catch-all, no distinguishing evidence signal, and the founder mandate's own Section 13 requires a real substantive-content gate first, which doesn't exist); no change to `_resolve_actor_context()` (extending it to use Phase 1G's `canonical_entities` for `threat_actor` was considered and deferred — real, separate, cross-module work); no touch to `regulatory_applicabilities`/`forecasts`/`hypothesis_sets` (confirmed structurally inert for every family, not a family-differentiation gap).

### 10.5 Test evidence

Root `tests/`: 469 → 475 passed, 0 failed. Engine `Sentinel-APEX/engine/tests/`: 970 → 980 passed, 1 pre-existing unrelated failure (same Node-rendering gap noted in §8/§11, confirmed untouched this round). `tests-js/`: 123 passed, unchanged (Pipeline B untouched). New dedicated adversarial-classification file `tests/test_report_integrity.py` (8 tests) closes a real, previously-zero test-coverage gap on `report_integrity._family()` — the single canonical classifier every other module trusts — against the founder mandate's own adversarial examples; all 8 passed on first run (the classifier was already correct, this closes the coverage gap without changing behavior).

## 11. Round 5 additions (Phase 1I — ATT&CK semantic validation, detection maturity, hunting)

Full detail: `docs/audits/REPORTX-PHASE1I-ATTACK-DETECTION-HUNTING-CERTIFICATION.md`.

### 11.1 What was already real before this round (corrects the mandate's own assumptions)

`detection_validation.DetectionValidationState` + `check_state_promotion()` already existed as a real, tested, ALREADY-HARD-GATED control (`commercial_readiness.py`'s `detection_evidence_discipline`, in `CORRECTNESS_CONTROLS`). `attack_mapper.map_techniques()` — negation-aware, evidence-anchored ATT&CK matching — already existed and was already used by `intelligence_validation.py`'s `_score_mitre_attack_justification()`, just as a display score, never a gate. `executive_products.HuntHypothesis` already existed, ~70% aligned with the mandate's own 12-field ask, tested by `_score_threat_hunting_guidance()`, but never called from `pipeline_composer.py`. None of this was rebuilt.

### 11.2 Detection-maturity vocabulary reconciled

`DetectionValidationState` gained `NOT_APPLICABLE` and `TELEMETRY_SPECIFICATION` (both off-ladder, same treatment as `WITHHELD_INSUFFICIENT_EVIDENCE`), and `pipeline_composer.py`'s status-mapping dict now covers all 4 real `report_renderer.DetectionPackage.status` strings instead of collapsing 3 of 4 into one.

### 11.3 ATT&CK-citation justification elevated from score to gate — with a real, empirically-caught design correction

The existing scorer's exact logic (validity + negation-aware textual evidence) was first ported as a hard-fail condition inside the existing `detection_evidence_discipline` control (not a new 24th row — this module's own docstring names "the exact 23-row matrix" as a fixed external contract). Run against real data for the first time, it correctly failed two real, already-published, gold-standard canary exports — not because their content was wrong, but because `attack_mapper.KNOWN_TECHNIQUES`'s curated registry was incomplete (missing T1219) and one rule cited a parent technique (T1053) where only the sub-technique (T1053.005) had real textual support. Hard-failing on curated-registry completeness was identified as a false-positive risk before merge and removed from the gate's fail condition; the registry gap itself was fixed (T1219 added; the one affected canary rule retargeted to the more precise T1053.005). Both real exports pass 23/23 again.

### 11.4 Real hunt hypotheses wired for `cve_advisory`

A genuine, evidence-grounded exposure-plus-exploitation hunt hypothesis (mandate Section 15's own example), reusing `_detection_package()`'s already-computed, vulnerability-class-conditioned telemetry guidance verbatim rather than re-deriving it. Reaches rendered HTML, the `intelligence_validation` scorecard (via a newly-constructed `SupplementalEvidence`, not previously passed at all), and `report_contract.py`'s Section 14 state (now `COMPLETE` for this family instead of permanently `WITHHELD_INSUFFICIENT_EVIDENCE` for every family, every article). Scoped to one family, matching the same "prove the pattern once, cleanly" discipline Round 4 used for family engines.

### 11.5 A real regression class, honestly framed

The mandate named specific historical incidents ("CodeWhale symlink/path traversal", "VMware/IIOP KEV") as regression canaries to reproduce. Neither was found anywhere in this repository (checked via repo-wide grep before writing any test) — treated as the mandate's own illustrative framing of a defect class, not a real prior incident, and the certification doc says so explicitly rather than fabricating a matching narrative. The two real defects this round *did* find (§11.3) are used as the actual regression-lock-in material instead, with tests that state precisely what was found and how, not a dramatized substitute.

### 11.6 Test evidence

Root `tests/`: 475 → 478 passed, 0 failed. Engine `Sentinel-APEX/engine/tests/`: 980 → 998 passed, 1 pre-existing unrelated failure (confirmed untouched). `tests-js/`: 123 passed, unchanged.

### 11.7 What remains (named, not attempted)

A formal structured ATT&CK object (`technique_id`/`tactic`/`status`/`evidence_refs`/`claim_refs`/`reasoning`/`confidence`/`behavioral_basis`/`limitations`) reaching the rendered report — today's mappings stay prose sentences, already conditionally-worded and already governed by a section-level disclaimer, but not formally structured. `cisa_kev`'s retrospective-exploitation hunt and every other family's hunting policy. Detection-maturity state-machine semantic QA beyond the promotion-language gate (e.g., a systematic audit of every vulnerability-class branch in `report_renderer._detection_package()` against the mandate's protocol/telemetry-mismatch concerns, Sections 10-11). None of this was started this round.
