# Intelligence Source-of-Truth Matrix

**Date:** 2026-08-24
**Purpose:** prevent a repeat of the campaign delivery integrity incident (a second, divergent state store silently drifting from the canonical one) by recording, per intelligence domain, exactly one canonical source and every derived consumer. Scoped to the live/automated intelligence-core domains this and the preceding Phase 0 audit round actually verified — not a claim of completeness across the entire platform (e.g. `docs/architecture/module-ownership.md`'s `lib/intelligence/*`/`lib/ioc/*` TypeScript layer is a separate system not re-audited here).

**Rule this document exists to enforce:** if you are about to add a second place that stores or derives data already covered by a row below, stop and either extend the canonical source or get an explicit, documented reason why not, before writing code — matching this repository's own `platform/open-issues.md` Issue 1 precedent (Python-vs-JS graph/scoring duplication), which is exactly the failure mode this table exists to prevent from recurring.

| Intelligence Type | Canonical Source | Derived Outputs | Persistence | Customer API |
|---|---|---|---|---|
| CVE / vulnerability intel | `api/_lib/threat-graph.js`'s `CVE`-type graph nodes, populated by `buildGraphFromIntel()` from `fetch-live-intel.js`'s live feed | Tier-filtered views via `getGraphForTier()`; per-CVE detail via `api/v1/intel.js`'s `getCVEDetail()` | `api/intel/threat-graph.json` (accumulates; correct, unchanged by this round) | `GET /api/v1/intel/cve/{id}`, embedded in `/graph` |
| Campaign | `api/_lib/threat-graph.js`'s `Campaign`-type graph nodes (identity + core attributes) **+** `api/_lib/campaign-engine.js`'s richer per-cycle projection, now correctly accumulated into `campaigns.json` via `mergeCampaigns()` (this round's fix) | `getCampaigns()`/`getCampaignDetail()` (`api/_lib/intel.js`) | `api/intel/campaigns.json` (now correctly accumulates — **this round's fix**; previously overwritten destructively every cycle, see the campaign delivery integrity v1 certification) | `GET /api/v1/intel/campaigns`, `GET /api/v1/intel/campaign/{id}` |
| Threat Actor | `api/_lib/threat-graph.js`'s hardcoded, curated `THREAT_ACTOR_DB` (8 actors, CISA/DOJ/vendor-sourced — deliberately not automated ingestion, given attribution requires human-verified evidence) | `getTopActors()`, actor attribution on every intel item via `computeActorAttribution()`'s 4-signal model | `api/intel/threat-graph.json` (`ThreatActor`-type nodes) | `GET /api/v1/intel/top-actors`, embedded `actor_attribution` on every intel item |
| IOC (indicator) | `api/_lib/threat-graph.js`'s `IOC`-type graph nodes, extracted per-item at ingestion (capped at 5 per item — a real, documented limit, see this round's campaign-reconstruction certification §9) | Embedded in graph/campaign responses; not (as of this audit) independently listable via a dedicated public IOC-feed endpoint distinct from the graph | `api/intel/threat-graph.json` | Embedded only — no standalone `GET /api/v1/intel/iocs` list endpoint was found wired to this same graph data (a separate `api/v1/ioc/search.js`/`api/v1/ioc/[id].js` pair exists — **not reconciled against this row; flagged as an open question, not assumed to be the same source**) |
| Malware | Schema-supported (`threat-graph.js`'s `normalizeNodeId()`/`computeStats()` both handle a `Malware` node type) but **zero populated nodes** — nothing in the current ingestion path ever creates one, despite actor descriptions referencing specific malware variants in free text. Unchanged, pre-existing, known gap (`platform/open-issues.md` Issue 8) | None yet | N/A | N/A |
| Correlation (`co_occurs_with` edges: Campaign↔Campaign, CVE↔CVE, Actor↔Actor) | `api/_lib/enrichment-pipeline.js`'s `linkCorrelated{Campaigns,CVEs,Actors}()`, computed additively from existing hierarchical edges (`includes`/`exploits`/`executes`) already in the graph | Embedded in `/graph` responses | `api/intel/threat-graph.json` (`co_occurs_with` edges — 62 live as of this audit) | Embedded in `GET /api/v1/intel/graph` |
| Detections (Sigma/KQL/Splunk/OSQuery, Suricata) | `engine-node/detection-engine.js` (live JS, 5-min cadence) — deliberately paired with `Sentinel-APEX/engine-node`'s parity Python port for the offline report pipeline (a documented, intentional dual-runtime, not duplication — see `platform/capabilities.md`) | N/A — this is itself the output layer | Generated files under the detection surface (not re-audited this round) | Paid-tier detection content, per `platform/capabilities.md` |
| Reports (hand-authored intelligence reports) | `Sentinel-APEX/renderer/` + `Sentinel-APEX/engine/` quality gate (Python), manually published via `publish-report.js` | Rendered HTML at `/intelligence/{slug}` | Committed report source + rendered output | Public report pages |
| SOC Workbench intelligence objects (`IntelligenceObject`) | `api/_lib/intelligence-manager.js`/`intelligence-object.js` — a **deliberately separate** system from every row above: disjoint ID scheme (`intel_<type>_<sha256>` vs. `actor:x`/`CVE-x`/`campaign:x`), Redis-persisted (not file-persisted), analyst-curated (not automated bot ingestion). See the Phase 0 audit's architecture graph for the full System A/B split | `api/v1/intelligence/*`, `api/v1/workbench/*` (internal, analyst-only) | Redis | Not customer-facing by design |

## Why Campaign has two contributing sources, not one

Unlike every other row, Campaign identity and core attributes live on the graph (`Campaign`-type nodes, accumulated via `addNode()`'s upsert, which was already correct before this round), while the *richer* per-campaign projection (full `related_intel[]` detail, `shared_iocs`, per-item flags) lives in the separate `campaigns.json` artifact built by `campaign-engine.js`. This is not accidental duplication in the Issue-1 sense (two independently-evolving implementations of the same capability) — the graph node is intentionally a lighter identity/attribute record, and `campaigns.json` is intentionally the richer, API-facing projection derived from it. The actual defect this round fixed was that the *derivation* direction had a one-way data-loss bug (the projection didn't accumulate the way its source did), not that two systems disagreed about what a campaign is. Verified post-fix: the two are now in exact ID-set agreement (`graph campaign IDs == campaigns.json campaign IDs`, checked directly against production data, both counts 1,187).

## Maintenance rule

Update this table in the same commit that changes which system owns a given intelligence type's canonical state, mirroring `platform/capabilities.md`'s own stated discipline (and its own admission that discipline has slipped before — see that file's "How to keep this current" section). A stale row here is exactly the kind of silent-drift risk this document exists to prevent.
