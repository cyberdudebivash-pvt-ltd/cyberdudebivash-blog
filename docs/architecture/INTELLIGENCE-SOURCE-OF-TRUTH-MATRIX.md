# Intelligence Source-of-Truth Matrix

**Date:** 2026-08-24
**Purpose:** prevent a repeat of the campaign delivery integrity incident (a second, divergent state store silently drifting from the canonical one) by recording, per intelligence domain, exactly one canonical source and every derived consumer. Scoped to the live/automated intelligence-core domains this and the preceding Phase 0 audit round actually verified — not a claim of completeness across the entire platform (e.g. `docs/architecture/module-ownership.md`'s `lib/intelligence/*`/`lib/ioc/*` TypeScript layer is a separate system not re-audited here).

**Rule this document exists to enforce:** if you are about to add a second place that stores or derives data already covered by a row below, stop and either extend the canonical source or get an explicit, documented reason why not, before writing code — matching this repository's own `platform/open-issues.md` Issue 1 precedent (Python-vs-JS graph/scoring duplication), which is exactly the failure mode this table exists to prevent from recurring.

| Intelligence Type | Canonical Source | Derived Outputs | Persistence | Customer API |
|---|---|---|---|---|
| CVE / vulnerability intel | `api/_lib/threat-graph.js`'s `CVE`-type graph nodes, populated by `buildGraphFromIntel()` from `fetch-live-intel.js`'s live feed | Tier-filtered views via `getGraphForTier()`; per-CVE detail via `api/v1/intel.js`'s `getCVEDetail()` | `api/intel/threat-graph.json` (accumulates; correct, unchanged by this round) | `GET /api/v1/intel/cve/{id}`, embedded in `/graph` |
| Campaign | `api/_lib/threat-graph.js`'s `Campaign`-type graph nodes (identity + core attributes) **+** `api/_lib/campaign-engine.js`'s richer per-cycle projection, now correctly accumulated into `campaigns.json` via `mergeCampaigns()` (this round's fix) | `getCampaigns()`/`getCampaignDetail()` (`api/_lib/intel.js`) | `api/intel/campaigns.json` (now correctly accumulates — **this round's fix**; previously overwritten destructively every cycle, see the campaign delivery integrity v1 certification) | `GET /api/v1/intel/campaigns`, `GET /api/v1/intel/campaign/{id}` |
| Threat Actor | `api/_lib/threat-graph.js`'s hardcoded, curated `THREAT_ACTOR_DB` (8 actors, CISA/DOJ/vendor-sourced — deliberately not automated ingestion, given attribution requires human-verified evidence) | `getTopActors()`, actor attribution on every intel item via `computeActorAttribution()`'s 4-signal model | `api/intel/threat-graph.json` (`ThreatActor`-type nodes) | `GET /api/v1/intel/top-actors`, embedded `actor_attribution` on every intel item |
| IOC (indicator) | `api/_lib/threat-graph.js`'s `IOC`-type graph nodes, extracted per-item at ingestion (capped at 5 per item — a real, documented limit, see this round's campaign-reconstruction certification §9) | Embedded in graph/campaign responses; **as of the Unified Intelligence Search v1 round, also `GET /api/v1/intel?action=ioc&id=...` and `action=unified-search&type=ioc`** (pro/enterprise only, matching the pre-existing `action=iocs` tier gate) | `api/intel/threat-graph.json` | `GET /api/v1/intel?action=ioc&id=...`, embedded in graph/campaign responses |
| **[RESOLVED, Unified Intelligence Search v1 round]** `api/v1/ioc/search.js` / `api/v1/ioc/[id].js` — **NOT the same source as the row above** | `data/ioc-canonical.json` — a **separate, disconnected, unauthenticated store**, confirmed via grep to have zero write callers anywhere in the live ingestion pipeline (`storeIOC()`/`storeIOCs()` have no external callers at all). Contains exactly 2 seed-looking records, stale since 2026-07-31 | `GET /api/v1/ioc/search`, `GET /api/v1/ioc/{id}` — reachable, but structurally near-empty and disconnected from the real 886-node IOC graph | `data/ioc-canonical.json` | Not reconciled with the graph in this round — flagged as a real, pre-existing gap (`platform/open-issues.md`), deliberately left unfixed (unauthenticated + credential/pipeline-adjacent, out of scope for this tranche) rather than silently patched |
| Malware | Schema-supported (`threat-graph.js`'s `normalizeNodeId()`/`computeStats()` both handle a `Malware` node type) but **zero populated nodes** — nothing in the current ingestion path ever creates one, despite actor descriptions referencing specific malware variants in free text. Unchanged, pre-existing, known gap (`platform/open-issues.md` Issue 8) | None yet | N/A | N/A |
| Correlation (`co_occurs_with` edges: Campaign↔Campaign, CVE↔CVE, Actor↔Actor) | `api/_lib/enrichment-pipeline.js`'s `linkCorrelated{Campaigns,CVEs,Actors}()`, computed additively from existing hierarchical edges (`includes`/`exploits`/`executes`) already in the graph | Embedded in `/graph` responses | `api/intel/threat-graph.json` (`co_occurs_with` edges — 62 live as of this audit) | Embedded in `GET /api/v1/intel/graph` |
| Detections (Sigma/KQL/Splunk/OSQuery, Suricata) | `engine-node/detection-engine.js` (live JS, 30-min cadence — corrected from a previously-stated "5-min," see `platform/capabilities.md`) — deliberately paired with `Sentinel-APEX/engine`'s parity Python port for the offline report pipeline (a documented, intentional dual-runtime, not duplication — see `platform/capabilities.md`) | N/A — this is itself the generation layer | `api/_lib/detection-rules.js`'s `data/detection-rules-canonical.json` (the canonical detection-rule **store**, distinct from the generator above) | Paid-tier detection content, per `platform/capabilities.md`; also `GET /api/v1/detections/rules[.js]` (pre-existing, unauthenticated — `platform/open-issues.md` Issue 29) |
| **Detection Coverage** (Threat-to-Defense Fabric v1) | **COVERAGE IS NOT CANONICAL INTELLIGENCE, NOT A SECOND DETECTION STORE.** Not a source in its own right — `api/_lib/detection-intelligence.js`'s `computeCoverage()` is a computed projection cross-referencing the canonical detection-rule store (row above) against the dossier's own already-canonical, evidence-graded `attack_context.techniques[]` (see the Intelligence Dossier row below) — never trusts a stored rule's own CVE/campaign claim without that independent corroboration. Deleting `detection-intelligence.js` and rebuilding its output from the two canonical sources it reads always reproduces the same result, matching the dossier's own "not canonical storage" discipline exactly | N/A — computed, not stored | None (in-memory only, per request — same as the dossier/search precedent, deliberately not a persisted index file) | `GET /api/v1/intel?action=detections\|detection\|detection-download\|detection-coverage\|detection-pack`, embedded in `action=dossier`'s `detections` section |
| **Customer Defense Profile** (Customer Telemetry & Environment-Aware Defense Coverage Fabric v1) | **A DEFENSE PROFILE IS CUSTOMER DECLARATION, NOT INTELLIGENCE.** `api/_lib/defense-profile-store.js` — a customer-owned record of explicitly declared SIEM/EDR/cloud/endpoint-telemetry technologies and per-data-source telemetry availability. Never inferred from IP/user-agent/DNS/company data/behavior (mandate's Absolute Trust Policy) — only what the customer explicitly saves. Ownership always re-derived server-side from `authenticate()`'s `userId`, matching `watchlist-store.js`'s exact precedent | N/A — this *is* the persistence layer for this domain | **Cloudflare D1** (`migrations/0003_defense_profiles.sql`, same `sentinel-apex-core` database): `defense_profiles` (PK `id`, UNIQUE `owner_id` — one profile per owner in v1), `defense_profile_technologies`, `defense_profile_telemetry` (a data source with no row means UNKNOWN, structurally — never a stored "unknown" status value), `defense_profile_audit_log` | `GET/POST /api/v1/defense-profile` (get/save/delete/taxonomy/entitlements) |
| **Customer-Specific Defense Coverage** (Customer Telemetry & Environment-Aware Defense Coverage Fabric v1) | **CUSTOMER COVERAGE IS A DERIVED STATE, NOT INDEPENDENT INTELLIGENCE TRUTH.** Not a source in its own right — `api/_lib/defense-compatibility.js`'s `computeCustomerCoverage()` wraps the unmodified global `computeCoverage()` (Detection Coverage row above) and cross-references each RELEASED detection's real requirements (`detection-engine.js`'s own `REGISTRY`/`FIELD_MAP` — the same values the KQL/Splunk/OSQuery generators already compile against, not a second mapping table) against the caller's own Defense Profile (row above). Global coverage truth is never modified by the presence or absence of a customer profile — verified by test (`customer coverage NEVER modifies global coverage truth`) | N/A — computed, not stored | None (in-memory only, per request — same "computed fresh, never persisted" discipline as the dossier/search/global-coverage precedent) | `GET /api/v1/intel?action=defense-coverage&type=cve\|campaign&id=...`, embedded in `dossier.html`'s "Your Defense Coverage" section |
| Reports (hand-authored intelligence reports) | `Sentinel-APEX/renderer/` + `Sentinel-APEX/engine/` quality gate (Python), manually published via `publish-report.js` | Rendered HTML at `/intelligence/{slug}` | Committed report source + rendered output | Public report pages |
| Published Reports manifest | `Sentinel-APEX/reports/published/*.md`'s own real YAML front matter (title/cves/attack_ids/severity/confidence/sources) — no other manifest existed before this round; `intelligence/index.html` is hand-authored HTML, not data, and had already drifted from reality once (`platform/open-issues.md` Issue 5) | `scripts/generate-reports-index.js` → `api/intel/reports-index.json` (3 records) | `api/intel/reports-index.json` | `GET /api/v1/intel?action=report&id=...`, embedded `report_refs[]` on cve/actor search documents |
| **Search projection** (Unified Intelligence Search v1) | Not a source in its own right — a computed view over the CVE/Campaign/Actor/IOC/Report rows above. `api/_lib/search-index.js`'s `buildSearchIndex()` derives it fresh, in-memory, on every 60s cache refresh; nothing here is ever persisted as an independently-mutable file. See that module's own header and the certification doc §11 for why this was a deliberate choice (eliminates the drift-risk class PR #130 had to guard against, rather than merely guarding against it) | N/A — computed, not stored | None (in-memory only) | `GET /api/v1/intel?action=unified-search`, `action=actor`, `action=ioc` |
| **CVE enrichment index** (Intelligence Dossiers v1) | `api/intel/cve/*.json` (1,765 per-CVE archive files) — a real, pre-existing, already-canonical Node/Vercel-only-reachable source. `scripts/generate-cve-enrichment-index.js` copies a lean, verbatim subset (EPSS score/percentile, source citations, scoring explanation, KEV due date/required action, report URL — nothing computed or invented) into one bundleable file, closing the gap where this data was unreachable on the canonical Cloudflare Workers runtime | `api/intel/cve-enrichment-index.json` | `api/intel/cve-enrichment-index.json` (rebuildable, deterministic) | Consumed by the dossier assembler only; not independently API-exposed |
| **Intelligence Dossier** (Intelligence Dossiers v1) | **DOSSIER IS NOT CANONICAL STORAGE.** Not a source in its own right — a computed, decision-oriented projection over the CVE/Campaign/CVE-enrichment/Reports rows above, assembled fresh per request by `api/_lib/intelligence-dossier.js`. Deleting it and rebuilding from those canonical sources always reproduces the same output. Supports `cve` and `campaign` entity types only — `actor`/`ioc`/`report`/`malware` are honestly unsupported (`UNSUPPORTED_ENTITY_TYPE`), not silently stubbed | N/A — computed, not stored | None (in-memory only, per request) | `GET /api/v1/intel?action=dossier&type=cve\|campaign&id=...`, `dossier.html` (customer-facing UI) |
| SOC Workbench intelligence objects (`IntelligenceObject`) + investigation/case/evidence/graph/analysis siblings | `api/_lib/intelligence-manager.js`/`intelligence-object.js` — a **deliberately separate** system from every row above: disjoint ID scheme (`intel_<type>_<sha256>` vs. `actor:x`/`CVE-x`/`campaign:x`), Redis-persisted (not file-persisted), analyst-curated (not automated bot ingestion). **Re-audited (Threat Hunting Workspace v1, 2026-08-29), confirmed still accurate and extended**: the same separation applies to its sibling modules discovered by that tranche's own reuse-before-build audit — `investigation-manager.js`, `case-manager.js`, `evidence-manager.js`, `timeline-engine.js`, `graph-engine.js`/`graph-traversal.js`/`correlation-engine.js`, and `analysis-manager.js` (structured-analytic findings, a distinct confidence vocabulary from this table's own Hunt Finding row below) — every one gated by an internal `X-Analyst-Key`, none with an `owner_id`/customer-tenancy concept, all Redis-persisted (now additionally barred for any NEW capability by `PRODUCTION-RUNTIME-POLICY.md`). A Hunt's `linked_case_reference` (Hunt row below) is a plain, analyst-entered text pointer into `case-manager.js` — never an automated bridge, never a shared identity. See the Phase 0 audit's architecture graph for the original System A/B split, and `docs/audits/SENTINEL-APEX-HUNTING-WORKSPACE-CAPABILITY-INVENTORY-V1.md` for the full re-audit | `api/v1/intelligence/*`, `api/v1/workbench/*`, `api/v1/analysis/*` (internal, analyst-only) | Redis | Not customer-facing by design |
| **Watchable state** (Watchlists v1) | **WATCHABLE STATE IS NOT CANONICAL INTELLIGENCE.** Not a source in its own right — `api/_lib/watchable-state.js` derives a small, deterministic, normalized projection (only the fields whose changes matter) from the exact same canonical sources the dossier assembler reads (the threat graph, `campaigns.json`, `reports-index.json`). CVE exploitation status and campaign confidence bucketing are reused directly from `intelligence-dossier.js`'s own `classifyExploitation()`/`campaignConfidenceBucket()`, so a dossier and a change event can never disagree | N/A — computed, not stored on its own | A fingerprint + this small state object are persisted per watched entity (`entity_snapshots` table, PK `(entity_type, entity_id)`, **Cloudflare D1** — migrated from the Redis `snapshot:{type}:{id}` key by the Cloudflare-Only Runtime Completion v2 tranche, 2026-08-26) as the minimal comparison basis the next evaluation needs — rebuildable from canonical sources at any time, never treated as independently-authoritative | Not directly API-exposed; consumed only by `api/_lib/change-engine.js` |
| **Intelligence change event** (Watchlists v1) | **A CHANGE EVENT IS A DERIVED FACT, NOT NEW INTELLIGENCE.** `api/_lib/change-detector.js`'s `detectChanges()` — a pure, deterministic diff between two watchable-state snapshots. No LLM determines whether a change occurred. Supports `cve` and `campaign` entities only, additions-only for relationship/KEV/exploitation changes (no reversal/removal event types in v1 — see `platform/open-issues.md` Issue 23) | `api/_lib/change-engine.js`'s `evaluateEntity()`/`evaluateWatchedEntities()` | `change_events` table (PK `event_id`, idempotent via `ON CONFLICT DO NOTHING`, indexed on `(entity_type, entity_id, observed_at)`), **Cloudflare D1** — migrated from the Redis `event:{event_id}` STRING + `events:by_entity:{type}:{id}` ZSET by the Cloudflare-Only Runtime Completion v2 tranche. The old `events:by_entity:*` ZSET was write-only (grep-confirmed never read anywhere) and was deliberately **not** replicated in D1 — a dead index removed, not carried forward as waste | `GET /api/v1/watchlists?action=feed` (via each customer's matched event references) |
| **Watchlist** (customer-owned subscription state) | **A WATCHLIST IS CUSTOMER INTENT, NOT INTELLIGENCE.** `api/_lib/watchlist-store.js` — a customer-owned collection of stable canonical entity references (never a duplicated dossier, never copied intelligence JSON). Ownership is always re-derived server-side from `authenticate()`'s `userId`, never trusted from the request body | N/A — this *is* the persistence layer for this domain | **Cloudflare D1** (`migrations/0002_watchlists_change_detection.sql`, migrated from Upstash Redis by the Cloudflare-Only Runtime Completion v2 tranche, 2026-08-26): `watchlists` table (PK `id`, indexed on `owner_id`), `watchlist_entities` table (composite PK `(watchlist_id, entity_type, entity_id)`, plain index on `(entity_type, entity_id)` for the reverse lookup — collapses the old Redis design's two mirrored SETs, `watchlist:{id}:entities` and `entity_watchers:{type}:{id}`, into one table), `owner_feed` table (composite PK `(owner_id, event_id)`, deliberately dedupes the case where one owner reaches the same event via two different watchlists — see the certification doc's concurrency proof), `watchlist_eval_state` (single-row cursor table, replacing the old `watchlist_eval:cursor` STRING key). Shares the `sentinel-apex-core` D1 database with the notification-delivery control plane row below (deliberate — see the certification doc §12) | `GET/POST /api/v1/watchlists` (list/create/get/update/delete/list-entities/add-entity/remove-entity/feed/entitlements) |
| **Notification / delivery state** (Alert Delivery & Webhook Automation v1, extended by Alert Orchestration & Delivery Reliability v1, migrated to D1 by Cloudflare-Only Alert Runtime v1, joined by watchlists/change-detection under Cloudflare-Only Runtime Completion v2) | **DELIVERY STATE IS NOT CANONICAL INTELLIGENCE.** Not a source in its own right — a transport/bookkeeping layer over the Intelligence change event row above. The causal chain: `Semantic Change Event (change-engine.js, Cloudflare D1) → Watchlist Match (watchlist-store.js, Cloudflare D1) → Customer Delivery Job (D1: notification_delivery_jobs, one row per (owner,event,channel)) → Delivery Attempt (per-job atomic claim/send/outcome) → Delivery History (D1: notification_delivery_log/notification_dead_letters)`. A delivery row only ever carries `event_id` by reference — the underlying event content is never copied into it, so a delivery record disagreeing with its source event is structurally impossible, not just avoided by convention | N/A — computed/transported, not a second copy of intelligence | **Migrated to Cloudflare D1** (`migrations/0001_notification_delivery.sql`, via `api/_lib/d1.js`) as of 2026-08-26 — see `docs/audits/SENTINEL-APEX-CLOUDFLARE-ONLY-ALERT-RUNTIME-V1-CERTIFICATION.md`. Tables: `notification_preferences` (PK `owner_id`), `notification_delivery_jobs` (PK `delivery_id`, one row per owner+event+channel, carries `state`/`claim_token`/`lease_expires_at` — the old `notify:claim:{deliveryId}` self-expiring Redis key is now a column on this same row, not a separate key), `notification_delivery_log`/`notification_dead_letters`/`notification_audit_log` (autoincrement, bounded by application-level trim on insert). **As of Cloudflare-Only Runtime Completion v2 (2026-08-26), the entire upstream chain also moved to the same shared `sentinel-apex-core` D1 database** — watchlists (`migrations/0002_watchlists_change_detection.sql`, see the Watchlist row above) and change events (same migration, see the Intelligence change event row above) are no longer Redis-backed. **Still Redis-backed**: only the customer-identity lookup `notification-dispatch.js`'s `getOwnerAccountEmail()` still performs directly against `user:id:*`/`user:key:*` (auth is LEGACY, audited and deliberately deferred — see `docs/audits/SENTINEL-APEX-AUTH-BILLING-DEFERRAL-AUDIT-V2.md`) | `GET/POST /api/v1/notifications` (preferences/rotate-secret/test-webhook/deliveries/dead-letters/retry-dead-letter) |

| **SIEM Connector** (Controlled SIEM Deployment Gateway v1) | **A CONNECTOR IS A CUSTOMER-OWNED CREDENTIAL RECORD, NOT INTELLIGENCE.** `api/_lib/siem-connector-store.js` — a customer-owned record of one SIEM platform target (e.g. a specific Microsoft Sentinel workspace) and its encrypted credential. Ownership always re-derived server-side from `authenticate()`'s `userId`, matching every other customer-owned store's exact precedent. Credential is AES-256-GCM envelope-encrypted (`api/_lib/connector-crypto.js`, mirroring `scripts/backup-customer-data.js`'s existing algorithm/format, not a second crypto convention) and never returned by any read path — only a `credential_configured` boolean is | N/A — this *is* the persistence layer for this domain | **Cloudflare D1** (`migrations/0004_siem_deployment_gateway.sql`, same `sentinel-apex-core` database): `siem_connectors` (PK `id`, indexed on `owner_id`), `siem_connector_audit_log` (capped, never records a credential value) | `GET/POST /api/v1/connectors` (platforms/entitlements/list/get/create/rotate-credential/test-connection/disable) |
| **Detection Deployment** (Controlled SIEM Deployment Gateway v1) | **A DEPLOYMENT RECORD IS DEPLOYMENT INTENT AND OBSERVED REMOTE STATE, NOT CANONICAL DETECTION TRUTH.** `api/_lib/deployment-engine.js`/`api/_lib/deployment-store.js` — one row per (connector, detection, entity) triple for its entire lifecycle (preview → approve → execute → verify → update → rollback → disable), never a second detection-content store: `query`/`severity_raw`/`technique_id` are always derived fresh from the canonical detection store (row above: Detections) at preview/approve/execute time, never accepted from a request body. The one deliberate exception: `deployed_intent_snapshot`/`previous_intent_snapshot` capture the exact content actually deployed, because the canonical detection store itself overwrites format content in place on every version bump (verified by direct reading of `detection-rules.js#storeRule()`) and so cannot answer "what did version 1 say" once version 2 is stored — this platform's own deployment record is the only place that survives, purely for its own rollback mechanism, never treated as an independently-authoritative second copy of the detection | N/A — computed at read time from canonical detection + compatibility state, except the rollback-only content snapshot above | **Cloudflare D1** (same migration): `detection_deployments` (PK `deployment_id`, indexed on `owner_id`/`connector_id`/`state`/`detection_id`), `deployment_approvals` (server-side approval records, never a client-supplied boolean), `deployment_attempts` (per-connector-dispatch attempt log), `deployment_audit_log` (capped) | `GET/POST /api/v1/deployments` (list/get/history/preview/approve/execute/verify/preview-rollback/disable) |
| **Remote SIEM resource / read-back state** (Controlled SIEM Deployment Gateway v1) | **REMOTE SIEM RESOURCE STATE = EXTERNALLY OBSERVED DEPLOYMENT STATE, NEVER THE CANONICAL DETECTION.** The connector's `readBack()` (`api/_lib/connectors/{mock-siem-connector,microsoft-sentinel-connector}.js`) is this platform's only view into what actually exists in the customer's own SIEM — canonicalized into one shared `{query,severity,enabled,techniques}` shape and hashed (`deployment-engine.js#canonicalizeForHash()`) for comparison against `desired_hash`. A mismatch is `DRIFTED`, never silently reconciled by overwriting the remote resource — the customer's SIEM remains the one source of truth for what is actually live there, this platform only observes and reports it | N/A — fetched live from the remote SIEM (or, for the Sandbox/Test connector, from the real, D1-persisted `mock_siem_resources` table that simulates one) on every read-back/verify call; not cached beyond the `observed_hash` column on `detection_deployments` | Simulated remote state for the Sandbox connector only: **Cloudflare D1** `mock_siem_resources` (same migration) — a real backing store so its state survives across the separate HTTP requests a deployment lifecycle spans, never a production customer-data table | Not independently API-exposed; surfaced only via `detection_deployments.state`/`observed_hash` through the Deployment row above |
| **Hunt** (Threat Hunting Workspace v1) | **A HUNT IS CUSTOMER-OWNED ANALYST WORKSPACE STATE, NOT CANONICAL INTELLIGENCE.** `api/_lib/hunt-store.js` — one hunt per analyst investigation, owned by a single customer (`owner_id` re-derived server-side, matching every other customer-owned store's exact precedent). A hunt's hypothesis/threat-refs/ATT&CK-refs are analyst-recorded pointers into the canonical CVE/Campaign/Actor/IOC/Detection sources elsewhere in this table, never a second copy of that intelligence — deleting a hunt never removes or alters the CVE/campaign/detection it referenced. **Deliberately not built on the pre-existing, Redis-backed SOC Workbench system** (`intelligence-manager.js`/`investigation-manager.js` row below) — that system has no customer-tenancy concept and `PRODUCTION-RUNTIME-POLICY.md` bars new Redis dependency for new capability; see `docs/audits/SENTINEL-APEX-HUNTING-WORKSPACE-CAPABILITY-INVENTORY-V1.md` | N/A — this *is* the persistence layer for this domain | **Cloudflare D1** (`migrations/0005_threat_hunting_workspace.sql`, same `sentinel-apex-core` database): `hunts` (PK `hunt_id`, indexed on `owner_id`/`status`), `hunt_refs` (polymorphic entity linkage, composite PK), `hunt_queries`, `hunt_observations`, `hunt_evidence_links`, `hunt_timeline` (capped audit log, matching every other audit-log table's exact pattern) | `GET/POST /api/v1/hunts` (list/get/create/update/close/reopen/add-ref/add-query/add-observation/add-evidence/timeline) |
| **Hunt telemetry readiness / coverage-maturity ladder** (Threat Hunting Workspace v1) | **NEITHER IS A SOURCE IN ITS OWN RIGHT — BOTH ARE COMPUTED PROJECTIONS.** Hunt readiness (`hunt-engine.js#computeHuntReadiness()`) wraps the unmodified `defense-compatibility.js#evaluateDetectionCompatibility()` per linked detection, rolled up hunt-wide. The coverage-maturity ladder (`AVAILABLE → ENVIRONMENT_COMPATIBLE → DEPLOYED → OBSERVED_SIGNAL → ANALYST_VALIDATED`, `hunt-engine.js#computeDetectionMaturity()`) is additive on top of, never replacing, the Compatibility states above — composes the Compatibility, Deployment, and Hunt Finding/Detection Feedback rows in this table, introduces zero new compatibility logic of its own | N/A — computed, not stored | None (in-memory only, per request — same "computed fresh, never persisted" discipline as Coverage/Customer Coverage/Watchable State above) | `GET /api/v1/hunts?action=get` (embedded `telemetry_readiness`), `action=detection-maturity` |
| **Hunt Finding** (Threat Hunting Workspace v1) | **HUNT FINDING = CUSTOMER-PRIVATE ANALYST STATE, NOT GLOBAL THREAT INTELLIGENCE AUTOMATICALLY.** A `hunt_findings` row (`CONFIRMED_MALICIOUS`/`LIKELY_MALICIOUS`/`BENIGN`/`EXPECTED_ACTIVITY`/`INCONCLUSIVE`/`FALSE_POSITIVE`/`NO_EVIDENCE_FOUND`, plus `HIGH`/`MEDIUM`/`LOW` confidence reusing `intelligence-object.js`'s existing vocabulary) is one customer's own analyst judgment about their own environment — it is never promoted into the canonical CVE/Campaign/Actor/Detection intelligence rows elsewhere in this table, and one customer's finding is never visible to, or aggregated for, another customer. `CONFIRMED_MALICIOUS` requires at least one linked `hunt_evidence_links` row (enforced in code, `hunt-engine.js#addFindingWithValidation()`), never asserted bare | N/A — this *is* the persistence layer for this domain | **Cloudflare D1** (same migration): `hunt_findings` (PK `finding_id`, indexed on `hunt_id`) | `GET /api/v1/hunts?action=findings`, `POST ...&action=add-finding` |
| **Detection Feedback** (Threat Hunting Workspace v1) | **DETECTION FEEDBACK IS TENANT-SCOPED SIGNAL, NEVER A SECOND DETECTION STORE, NEVER AUTOMATICALLY GLOBALIZED.** `api/_lib/detection-feedback-store.js` — pinned to `(detection_id, detection_version)`, always `owner_id`-scoped for storage and individual retrieval; one customer's `FALSE_POSITIVE` never means the detection is globally invalid. The one deliberate exception is `computeFeedbackSignal()`, a cross-tenant AGGREGATE-ONLY read (never a raw row, `owner_id`, or free-text summary — verified by test) that sets a computed `REVIEW_REQUIRED` signal on the shared detection when a single `QUERY_ERROR`/`TELEMETRY_MISMATCH` report or 3+-distinct-owner `TOO_BROAD`/`TOO_NARROW` reports exist — this signal is an additional, non-mutating annotation, never a write to `detection-rules.js`'s own canonical row, and never auto-rewrites/auto-releases/auto-revokes the detection | N/A — this *is* the persistence layer for this domain; the aggregate signal itself is computed fresh from it on every call, never separately stored | **Cloudflare D1** (same migration): `detection_feedback` (PK `feedback_id`, indexed on `owner_id` and on `(detection_id, detection_version)`) | `GET/POST /api/v1/hunts` (`feedback-list`/`feedback-signal`/`feedback-submit`) |
| **Detection Version Snapshot** (Detection Performance Intelligence v1) | **A VERSION SNAPSHOT IS AN IMMUTABLE CONTENT RECORD, NEVER A SECOND MUTABLE DETECTION STORE.** `api/_lib/detection-version-store.js` — one row per (detection_id, version) that has ever been captured, written once via `INSERT ... ON CONFLICT DO NOTHING` and never updated or deleted at the application layer. Closes a real, already-occurred defect: `detection-rules.js#storeRule()` (the row above, Detections) overwrites content in place on every version bump, so the canonical store itself can only ever answer "what does the CURRENT version say" — this table is now the only place a past version's actual query content survives, captured via a minimal, additive, fire-and-forget hook inside `storeRule()` (never awaited, so `storeRule()`'s required synchronous signature for its one real caller, `fetch-live-intel.js#genMultiPlatformDetections()`, is unchanged) plus a one-time backfill of the 5 pre-existing rules' current content. Content from before this tranche's backfill is honestly marked unavailable, never invented — see `docs/audits/SENTINEL-APEX-DETECTION-PERFORMANCE-INTELLIGENCE-V1-CERTIFICATION.md` | N/A — this *is* the persistence layer for this domain | **Cloudflare D1** (`migrations/0006_detection_performance_intelligence.sql`, same `sentinel-apex-core` database): `detection_versions` (composite PK `(detection_id, version)`, indexed on `detection_id`) | `GET /api/v1/detections/performance?action=version-history` |
| **Tenant Performance Record** (Detection Performance Intelligence v1) | **NOT A SOURCE IN ITS OWN RIGHT — A COMPUTED, OWNER-SCOPED PROJECTION.** `detection-feedback-store.js#computeTenantPerformance()` derives one customer's own operational-feedback counts for one (detection, version) fresh, on demand, from the Detection Feedback row above — never a second, materialized counter that could drift from the raw feedback it summarizes. Owner-scoped exactly like every function in that file except the one deliberate cross-tenant exception below | N/A — computed, not stored | None (in-memory only, per request — matching this table's recurring "coverage/signal is never persisted" discipline) | `GET /api/v1/detections/performance?action=my-performance` (caller's own data only) |
| **Detection Quality State & Review Priority** (Detection Performance Intelligence v1) | **QUALITY STATE IS A DERIVED PRODUCT STATE, NOT RAW CUSTOMER INTELLIGENCE, NOT A SECOND DETECTION STORE, AND NEVER AN ML SCORE.** `api/_lib/detection-performance-engine.js` composes: (1) the Detection row above's own canonical validation gate (via `hunt-engine.js#resolveCanonicalDetection()`, unmodified) for a context-independent `BLOCKED` signal, plus (2) a NEW, additive, privacy-safe GLOBAL aggregate (`detection-feedback-store.js#computeGlobalReviewMetrics()`, composing the existing `computeFeedbackSignal()` unchanged with one more count-only query) for cross-tenant `QUERY_ERROR`/`TELEMETRY_MISMATCH`/repeated-`TOO_BROAD`/`TOO_NARROW` signal. The resulting Quality State (`INSUFFICIENT_EVIDENCE`/`HEALTHY`/`TUNING_RECOMMENDED`/`REVIEW_REQUIRED`/`TECHNICAL_FAILURE`/`REVOKED`/`DEPRECATED`) and Review Priority are deterministic functions of these inputs with an explicit, human-readable reason attached — never a probability, never a fabricated precision/recall/false-positive-rate statistic (see the certification doc's Statistical Truth Policy). Full chain this row completes: Released Detection Version → Customer Deployment → Threat Hunt → Detection Feedback → Tenant Performance State → Privacy-Safe Aggregate → **Detection Quality State** → Review/Tuning Candidate (a human-actioned recommendation only — this row can never write back into the Detection Version Snapshot or Detections rows above; a new version still requires an explicit `storeRule()` call through the existing pipeline, and release/deployment still require the existing validation gate and PR #143 approval flow) | N/A — computed, not stored; see `docs/architecture/DETECTION-FEEDBACK-PRIVACY-MODEL.md` for exactly what may vs. may never cross the aggregation boundary | None (in-memory only, per request) | `GET /api/v1/detections/performance` (`action=quality`, authenticated; `action=review-queue`, internal `X-Admin-Key` only) |

## Why Campaign has two contributing sources, not one

Unlike every other row, Campaign identity and core attributes live on the graph (`Campaign`-type nodes, accumulated via `addNode()`'s upsert, which was already correct before this round), while the *richer* per-campaign projection (full `related_intel[]` detail, `shared_iocs`, per-item flags) lives in the separate `campaigns.json` artifact built by `campaign-engine.js`. This is not accidental duplication in the Issue-1 sense (two independently-evolving implementations of the same capability) — the graph node is intentionally a lighter identity/attribute record, and `campaigns.json` is intentionally the richer, API-facing projection derived from it. The actual defect this round fixed was that the *derivation* direction had a one-way data-loss bug (the projection didn't accumulate the way its source did), not that two systems disagreed about what a campaign is. Verified post-fix: the two are now in exact ID-set agreement (`graph campaign IDs == campaigns.json campaign IDs`, checked directly against production data, both counts 1,187).

## Maintenance rule

Update this table in the same commit that changes which system owns a given intelligence type's canonical state, mirroring `platform/capabilities.md`'s own stated discipline (and its own admission that discipline has slipped before — see that file's "How to keep this current" section). A stale row here is exactly the kind of silent-drift risk this document exists to prevent.
